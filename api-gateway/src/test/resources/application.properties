# Test: stub values so context loads without external services (CI).
# Auth service URL — dummy; WebClient only connects on first JWT validation.
auth.service.url=http://localhost:8081

spring.cloud.discovery.enabled=false
